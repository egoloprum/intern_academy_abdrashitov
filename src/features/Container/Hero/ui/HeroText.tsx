import styles from "../styles/hero.module.css"

const HeroText = ({}) => {
  return (
    <section className={styles.heroText}>
      <p>EST 21.12.24</p>
      <p>A place where design philosophy blends innovation and clarity to create impactful solutions</p>
    </section>
  )
}

export default HeroText

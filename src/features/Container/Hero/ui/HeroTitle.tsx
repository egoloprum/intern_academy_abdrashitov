import styles from "../styles/hero.module.css"

const HeroTitle = ({}) => {
  return (
    <section className={styles.heroTitle}>
      <p>The Art of Bold Precision</p>
    </section>
  )
}

export default HeroTitle

import styles from '../styles/about.module.css'

export const Title = ({}) => {
  return (
    <section className={`${styles.textTitle} bigTextSize`}>
      <p>ABOUT SCHRIFT</p>
    </section>
  )
}

